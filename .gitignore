node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
/.gsk/
/opencode.json
/gemini-extension.json

# Local backend configuration, uploads and database state
/backend/config/local.php
/backend/storage/*
!/backend/storage/.gitkeep

/backend/vendor/
/artifacts/
